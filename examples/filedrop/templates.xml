<?xml version="1.0" encoding="UTF-8"?>
<messages>
  <message id="FileDropApplication.template">
    <h1>Try dropping a file in the widget below</h1>

    ${dropwidget}

    <div>
      ${select-file-btn} ${select-dir-btn}
    </div>

    <div style="height: 20px;">
      ${<if:progress>}
      ${progress-msg} ${abort-btn}
      ${</if:progress>}
    </div>

    <h4>Event Log:</h4>
    ${log class='log'}
  </message>
</messages>
